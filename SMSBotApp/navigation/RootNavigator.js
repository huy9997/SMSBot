import { createStackNavigator } from "react-navigation";

const createRootNavigator = () => {
  return createStackNavigator({
    // introstack for intro/login/sign up
  });
};

export default createRootNavigator;
